# 3.1. 시간개념을 적용한 무기추천 프로세스 모형 정립

## 3.1.1. 위협 평가(Threat Assessment)

### ○ 위협 종류 및 특성 분석

#### ▪ 탄도미사일 종류별 분류

본 시스템은 북한의 주요 탄도미사일 위협을 사거리 기준으로 분류하여 대응한다.

**1. CRBM (Close-Range Ballistic Missile) - Scud-B**
```
사거리: 300km
최대 고도: 40km
비행 속도: 마하 2.5 (약 850m/s)
비행 시간: 약 240초 (4분)
페이로드: 985kg
원형공산오차(CEP): 450m
```

**2. MRBM (Medium-Range Ballistic Missile) - 노동**
```
사거리: 1,300km
최대 고도: 80km
비행 속도: 마하 3.5 (약 1,200m/s)
비행 시간: 약 480초 (8분)
페이로드: 800kg
원형공산오차(CEP): 2,000m
```

**3. IRBM (Intermediate-Range Ballistic Missile)**
```
사거리: 3,000~5,500km
최대 고도: 100km 이상
비행 속도: 마하 5.0 이상
비행 시간: 600초 이상
```

**코드 구현** (`@config_mip.py:349-540`):
```python
# 노동 미사일 스펙
"NODONG": {
    "max_range_km": 1300,
    "max_altitude_km": 80,
    "speed_mach": 3.5,
    "payload_kg": 800,
    "cep_m": 2000,
    "launch_weight_kg": 16500
}

# Scud-B 미사일 스펙
"SCUD_B": {
    "max_range_km": 300,
    "max_altitude_km": 40,
    "speed_mach": 2.5,
    "payload_kg": 985,
    "cep_m": 450,
    "launch_weight_kg": 5900
}
```

#### ▪ 사거리 범위 및 대응 체계

**상층방어체계 (L-SAM)**
- 교전 고도: 40~80km
- 교전 거리: 150~300km
- 대응 대상: MRBM, IRBM (중·장거리 탄도탄)
- 동시 교전 능력: 10개 표적
- 포대당 탄약: 24발 (발사대 4개 × 6발)

**하층방어체계 (M-SAM)**
- 교전 고도: 0.05~50km
- 교전 거리: 5~50km
- 대응 대상: CRBM, MRBM (단·중거리 탄도탄)
- 동시 교전 능력: 8개 표적
- 포대당 탄약: 48발 (발사대 6개 × 8발)

**코드 구현** (`@config_mip.py:34-104`):
```python
@staticmethod
def get_lsam_specs() -> Dict:
    """L-SAM (장거리 지대공 미사일) 실제 스펙"""
    return {
        "ballistic_missile_specs": {
            "engagement_altitude_km": {"min": 40, "max": 80},
            "engagement_range_km": {"min": 150, "max": 300},
            "simultaneous_engagements": 10,
            "intercept_probability_single": 0.85,
            "intercept_probability_salvo": 0.9775,
            "missiles_per_engagement": 2
        },
        "battery_config": {
            "launchers": 4,
            "missiles_per_launcher": 6,
            "total_missiles": 24,
            "simultaneous_engagements": 3
        }
    }

@staticmethod
def get_msam_specs() -> Dict:
    """M-SAM (중거리 지대공 미사일) 실제 스펙"""
    return {
        "ballistic_missile_specs": {
            "engagement_altitude_km": {"min": 0.05, "max": 50},
            "engagement_range_km": {"min": 5, "max": 50},
            "simultaneous_engagements": 8,
            "intercept_probability_single": 0.80,
            "intercept_probability_salvo": 0.96,
            "missiles_per_engagement": 2
        },
        "battery_config": {
            "launchers": 6,
            "missiles_per_launcher": 8,
            "total_missiles": 48,
            "simultaneous_engagements": 3
        }
    }
```

#### ▪ 비행 궤적 특성 (3단계 비행 모델)

탄도미사일의 비행 궤적은 물리 법칙에 따라 3단계로 구분되며, 각 단계별로 교전 가능성과 요격 확률이 상이하다. 본 시스템은 단계별 특성을 정밀하게 모델링하여 최적 교전 시점을 도출한다.

---

##### **1단계: 부스트 단계 (Boost Phase)**

**물리적 특성**:
```
시간 구간: T_boost ∈ [0, t_burnout]
  - MRBM (노동): 0~60초
  - CRBM (Scud-B): 0~45초

고도 범위: h(t) ∈ [0, h_burnout]
  - MRBM: 0~20km
  - CRBM: 0~15km

가속도 프로파일:
  a(t) = (F_thrust - m(t)·g - D(v,h)) / m(t)
  
  where:
    F_thrust: 추력 (시간에 따라 감소)
    m(t): 질량 (연료 소모로 감소)
    g: 중력 가속도 (9.81 m/s²)
    D(v,h): 공기 저항 (속도, 고도 종속)
```

**단계별 역학 방정식**:
```
위치: r(t) = r₀ + ∫∫ a(τ) dτ²
속도: v(t) = v₀ + ∫ a(τ) dτ
질량: m(t) = m₀ - ṁ·t  (연료 소모율 ṁ)
```

**교전 제약 조건**:
```
1. 탐지 지연: 발사 후 최소 5~10초 소요
2. 추적 안정화: 레이더 Lock-on까지 추가 5~10초
3. 고속 기동: 급격한 가속으로 예측 오차 증가
4. 대기권 내 비행: 높은 공기 저항으로 요격체 성능 저하

→ 결론: 부스트 단계 교전은 기술적으로 불가능
```

**시스템 처리 방식**:
```
부스트 단계에서는 교전을 시도하지 않고, 궤적 추정 및 다음 단계 준비에 집중한다.

- 레이더 추적 데이터 수집
- 발사 지점 역추적
- 목표 자산 예측
- 중간코스 단계 교전 준비
```

---

##### **2단계: 중간 코스 단계 (Midcourse Phase)**

**물리적 특성**:
```
시간 구간: T_midcourse ∈ [t_burnout, t_terminal]
  - MRBM (노동): 60~320초 (260초 지속)
  - CRBM (Scud-B): 45~180초 (135초 지속)

고도 범위: h(t) ∈ [h_burnout, h_apogee, h_terminal]
  - MRBM 최대 고도: 80km (정점)
  - CRBM 최대 고도: 40km (정점)

궤적 특성: 탄도 비행 (Ballistic Trajectory)
  - 엔진 정지 상태
  - 중력과 공기 저항만 작용
  - 포물선 궤적 (대기권 밖에서는 타원 궤도)
```

**탄도 궤적 수학 모델**:
```
운동 방정식 (대기권 밖):
  d²r/dt² = -μ/r³ · r

  where:
    μ = G·M_earth (지구 중력 상수)
    r: 지구 중심으로부터의 위치 벡터

포물선 근사 (대기권 내):
  x(t) = x₀ + v_x·t
  y(t) = y₀ + v_y·t - ½g·t²
  h(t) = h₀ + v_h·t - ½g·t²

최대 고도 도달 시간:
  t_apogee = v_h / g
  h_max = h₀ + v_h² / (2g)
```

**교전 최적화 이론**:
```
중간코스 단계는 상층방어(L-SAM)의 주요 교전 영역이다.

최적 교전 시점 결정 기준:

1. 궤적 예측 정확도 최대화
   - 탄도 궤적 안정화 (엔진 정지 후 충분한 시간 경과)
   - 레이더 추적 데이터 축적
   
2. 교전 시간 창 최대화
   - 미사일이 L-SAM 교전 영역 내 체류 시간 극대화
   - 고도 40~80km 구간 통과 시간 활용
   
3. 요격 확률 최대화
   - 최적 고도: 55km (L-SAM 최적 교전 고도)
   - 최적 거리: 150~200km (최대 범위의 60~70%)
   - 최적 각도: 정면 요격 (Head-on Engagement)

수학적 정식화:
  t_optimal = argmax_{t∈T_midcourse} [W(t) × P_k(t) × Q(t)]
  
  where:
    W(t): 교전 시간 창 품질
    P_k(t): 요격 확률
    Q(t): 궤적 예측 품질
```

**단계별 교전 윈도우 계산**:
```
교전 가능 시작 시점:
  t_start = t_burnout + Δt_stabilization
  
  where:
    Δt_stabilization: 궤적 안정화 시간 (10~20초)

교전 가능 종료 시점:
  t_end = t_terminal - Δt_margin
  
  where:
    Δt_margin: 안전 여유 시간 (30~40초)

최적 교전 윈도우:
  T_optimal = [t_apogee - 30초, t_apogee + 30초]
  
  이론적 근거:
    - 정점 부근에서 속도 최소 (v_vertical ≈ 0)
    - 궤적 예측 오차 최소
    - 요격체 상대 속도 최적화
```

**L-SAM 교전 영역 매칭**:
```
L-SAM 교전 조건:
  40km ≤ h(t) ≤ 80km  (고도 조건)
  150km ≤ d(t) ≤ 300km  (거리 조건)

노동 미사일 (MRBM) 사례:
  - 비행 시간: 480초
  - 정점 도달: t = 240초, h = 80km
  - L-SAM 교전 가능 구간: t ∈ [160초, 320초] (160초 지속)
  - 최적 교전 시점: t = 240초 ± 30초

Scud-B 미사일 (CRBM) 사례:
  - 비행 시간: 240초
  - 정점 도달: t = 120초, h = 40km
  - L-SAM 교전 가능 구간: t ∈ [80초, 160초] (80초 지속)
  - 최적 교전 시점: t = 120초 ± 20초
```

---

##### **3단계: 종말 단계 (Terminal Phase)**

**물리적 특성**:
```
시간 구간: T_terminal ∈ [t_terminal, t_impact]
  - MRBM (노동): 320~480초 (160초 지속)
  - CRBM (Scud-B): 180~240초 (60초 지속)

고도 범위: h(t) ∈ [h_terminal, 0]
  - 시작 고도: 40~50km
  - 종료 고도: 0km (지표면)

속도 프로파일:
  v(t) = v_terminal + ∫ (g - D(v,h)/m) dt
  
  종말 속도:
    - MRBM: 1,800~2,000 m/s (마하 5.3~5.9)
    - CRBM: 1,200~1,400 m/s (마하 3.5~4.1)
```

**대기권 재진입 역학**:
```
재진입 각도: γ_entry ∈ [-30°, -60°]
  - 얕은 각도: 긴 체류 시간, 낮은 가속도
  - 가파른 각도: 짧은 체류 시간, 높은 가속도

공기 저항 증가:
  D(v,h) = ½ρ(h)·v²·C_d·A
  
  where:
    ρ(h): 대기 밀도 (고도 감소 시 지수적 증가)
    C_d: 항력 계수 (0.3~0.5)
    A: 단면적

열 부하:
  Q̇ = k·ρ^n·v^m
  
  where:
    k, n, m: 상수 (미사일 형상 종속)
    고도 20km 이하에서 급격히 증가
```

**교전 제약 조건**:
```
1. 시간 제약:
   - 짧은 교전 시간 창 (60~160초)
   - 빠른 의사결정 요구 (10초 이내)
   
2. 속도 제약:
   - 높은 종말 속도 (마하 3.5~5.9)
   - 요격체 상대 속도 증가
   - 명중 난이도 상승
   
3. 기동 제약:
   - 일부 미사일은 종말 기동 수행
   - 예측 오차 증가
   - 재목표 가능성
```

**M-SAM 교전 최적화**:
```
M-SAM 교전 조건:
  0.05km ≤ h(t) ≤ 50km  (고도 조건)
  5km ≤ d(t) ≤ 50km  (거리 조건)

최적 교전 전략:

1. 조기 교전 (Early Terminal):
   - 고도: 30~50km
   - 장점: 상대적으로 낮은 속도, 긴 교전 시간
   - 단점: 상층방어 실패 시에만 가능
   
2. 중기 교전 (Mid Terminal):
   - 고도: 15~30km
   - 장점: 균형잡힌 조건
   - 단점: 중간 수준의 난이도
   
3. 후기 교전 (Late Terminal):
   - 고도: 5~15km
   - 장점: 최종 방어선
   - 단점: 높은 속도, 짧은 시간, 높은 난이도

최적 교전 시점:
  t_optimal_MSAM = t_terminal + (t_impact - t_terminal) × 0.4
  
  이론적 근거:
    - 충분한 교전 시간 확보 (60% 남음)
    - 상층방어 실패 확인 가능
    - 재교전 기회 보장
```

---

##### **단계 전환 동역학 (Phase Transition Dynamics)**

**부스트 → 중간코스 전환**:
```
전환 조건:
  1. 엔진 연소 종료 (Burnout)
  2. 추력 = 0
  3. 가속도 = -g (중력만 작용)

전환 시점 탐지:
  - 레이더 추적: 가속도 급감 관측
  - 적외선 센서: 플룸 소멸 감지
  - 궤적 분석: 등가속도 운동 → 등속도 운동

전환 시간:
  t_burnout = m₀·I_sp·g / F_thrust
  
  where:
    I_sp: 비추력 (Specific Impulse)
    m₀: 초기 질량
```

**중간코스 → 종말 전환**:
```
전환 조건:
  1. 고도 h(t) ≤ h_terminal (40~50km)
  2. 대기 밀도 증가 감지
  3. 공기 저항 영향 유의미

전환 시점 결정:
  t_terminal = t_apogee + Δt_descent
  
  where:
    Δt_descent: 정점에서 종말 고도까지 하강 시간

전환 기준:
  - MRBM: h = 50km (비행시간의 2/3 지점)
  - CRBM: h = 40km (비행시간의 3/4 지점)
```

---

##### **단계별 요격 체계 매칭 이론**

**상층방어(L-SAM) - 중간코스 단계 최적화**:
```
매칭 원리:
  L-SAM 교전 고도 [40~80km] ⊂ 중간코스 고도 범위
  
최적 교전 조건:
  1. 고도 매칭: h_optimal = 55km (교전 영역 중심)
  2. 거리 매칭: d_optimal = 175km (최대 범위의 58%)
  3. 시간 매칭: t_optimal = t_apogee ± 30초

교전 성공 확률 최대화:
  P_success(t) = P_detection(t) × P_tracking(t) × P_intercept(t)
  
  중간코스 단계에서:
    P_detection ≈ 0.98 (레이더 추적 안정)
    P_tracking ≈ 0.95 (예측 가능한 궤적)
    P_intercept ≈ 0.85 (단발 요격 확률)
    
    → P_success ≈ 0.79 (단발 기준)
    → P_success_salvo ≈ 0.96 (2발 살보 기준)
```

**하층방어(M-SAM) - 종말 단계 최적화**:
```
매칭 원리:
  M-SAM 교전 고도 [0.05~50km] ⊂ 종말 단계 고도 범위
  
최적 교전 조건:
  1. 고도 매칭: h_optimal = 25km (교전 영역 중심)
  2. 거리 매칭: d_optimal = 30km (최대 범위의 60%)
  3. 시간 매칭: t_optimal = t_terminal + 0.4×(t_impact - t_terminal)

교전 성공 확률 고려사항:
  P_success(t) = P_detection(t) × P_tracking(t) × P_intercept(t) × P_no_maneuver(t)
  
  종말 단계에서:
    P_detection ≈ 0.95 (레이더 추적 유지)
    P_tracking ≈ 0.85 (높은 속도로 예측 오차 증가)
    P_intercept ≈ 0.78 (단발 요격 확률, 속도 고려)
    P_no_maneuver ≈ 0.90 (종말 기동 가능성)
    
    → P_success ≈ 0.57 (단발 기준)
    → P_success_salvo ≈ 0.82 (2발 살보 기준)
```

---

##### **시스템 구현: 단계별 처리 알고리즘**

**단계 식별 알고리즘**:
```
def identify_missile_phase(threat, current_time):
    """미사일 현재 단계 식별"""
    
    elapsed_time = current_time - threat.launch_time
    flight_progress = elapsed_time / threat.flight_time
    
    # 단계 1: 부스트 단계
    if elapsed_time < threat.burnout_time:
        return "BOOST"
    
    # 단계 3: 종말 단계
    elif flight_progress >= threat.terminal_phase_ratio:
        return "TERMINAL"
    
    # 단계 2: 중간코스 단계
    else:
        return "MIDCOURSE"
```

**단계별 교전 가능성 평가**:
```
def evaluate_engagement_feasibility(threat, battery, current_time):
    """단계별 교전 가능성 평가"""
    
    phase = identify_missile_phase(threat, current_time)
    
    if phase == "BOOST":
        # 부스트 단계: 교전 불가
        return {
            "can_engage": False,
            "reason": "Boost phase - tracking unstable"
        }
    
    elif phase == "MIDCOURSE":
        # 중간코스 단계: L-SAM 최적
        if battery.system_type == "LSAM":
            altitude = calculate_altitude(threat, current_time)
            distance = calculate_distance(threat, battery, current_time)
            
            if 40 <= altitude <= 80 and 150 <= distance <= 300:
                return {
                    "can_engage": True,
                    "phase": "MIDCOURSE",
                    "optimality": "OPTIMAL",
                    "window_quality": calculate_window_quality(threat, battery)
                }
        
        # M-SAM은 중간코스에서 교전 가능하나 비최적
        elif battery.system_type == "MSAM":
            return {
                "can_engage": True,
                "phase": "MIDCOURSE",
                "optimality": "SUBOPTIMAL",
                "reason": "Reserve for terminal phase"
            }
    
    elif phase == "TERMINAL":
        # 종말 단계: M-SAM 최적
        if battery.system_type == "MSAM":
            altitude = calculate_altitude(threat, current_time)
            distance = calculate_distance(threat, battery, current_time)
            
            if 0.05 <= altitude <= 50 and 5 <= distance <= 50:
                return {
                    "can_engage": True,
                    "phase": "TERMINAL",
                    "optimality": "OPTIMAL",
                    "window_quality": calculate_window_quality(threat, battery)
                }
        
        # L-SAM은 종말 단계에서 교전 불가 (고도 제약)
        elif battery.system_type == "LSAM":
            return {
                "can_engage": False,
                "reason": "Terminal phase - below minimum altitude"
            }
    
    return {"can_engage": False}
```

**궤적 계산 알고리즘** (단계별 정밀 모델):
```python
def calculate_trajectory_intercept_point(threat: Dict, intercept_time: float) -> Dict:
    """미사일 궤적에서 교전 시점의 위치와 고도 계산 (단계별 모델)"""
    
    elapsed_time = intercept_time - threat["launch_time"]
    flight_time = threat["flight_time"]
    time_ratio = elapsed_time / flight_time
    
    # 단계 식별
    if elapsed_time < threat.get("burnout_time", 60):
        phase = "BOOST"
    elif time_ratio >= threat.get("terminal_phase_ratio", 0.67):
        phase = "TERMINAL"
    else:
        phase = "MIDCOURSE"
    
    # 수평 위치 (모든 단계 공통: 선형 보간)
    launch_pos = threat["launch_position"]
    target_pos = get_target_position(threat["target_asset_id"])
    
    intercept_x = launch_pos[0] + (target_pos[0] - launch_pos[0]) * time_ratio
    intercept_y = launch_pos[1] + (target_pos[1] - launch_pos[1]) * time_ratio
    
    # 고도 계산 (단계별 차별화)
    critical_altitude = threat.get("critical_altitude_km", 50.0)
    
    if phase == "BOOST":
        # 부스트 단계: 지수적 상승
        burnout_time = threat.get("burnout_time", 60)
        boost_ratio = elapsed_time / burnout_time
        altitude = critical_altitude * 0.25 * (1 - np.exp(-3 * boost_ratio))
        
    elif phase == "MIDCOURSE":
        # 중간코스 단계: 포물선 궤적
        # 정점 시간 = 비행시간의 50%
        apogee_ratio = 0.5
        
        if time_ratio <= apogee_ratio:
            # 상승기: 0 → critical_altitude
            normalized_ratio = time_ratio / apogee_ratio
            altitude = critical_altitude * (2 * normalized_ratio - normalized_ratio**2)
        else:
            # 하강기: critical_altitude → terminal_altitude
            terminal_ratio = threat.get("terminal_phase_ratio", 0.67)
            terminal_altitude = critical_altitude * 0.8
            
            descent_progress = (time_ratio - apogee_ratio) / (terminal_ratio - apogee_ratio)
            altitude = critical_altitude - (critical_altitude - terminal_altitude) * descent_progress
    
    else:  # TERMINAL
        # 종말 단계: 급격한 하강 (지수적 감소)
        terminal_ratio = threat.get("terminal_phase_ratio", 0.67)
        terminal_altitude = critical_altitude * 0.8
        
        descent_progress = (time_ratio - terminal_ratio) / (1.0 - terminal_ratio)
        altitude = terminal_altitude * np.exp(-2 * descent_progress)
    
    # 속도 계산 (단계별)
    if phase == "BOOST":
        velocity = threat.get("burnout_velocity", 1500) * boost_ratio
    elif phase == "MIDCOURSE":
        velocity = threat.get("midcourse_velocity", 2000)
    else:  # TERMINAL
        terminal_velocity = threat.get("terminal_velocity_ms", 1800)
        velocity = terminal_velocity * (0.7 + 0.3 * descent_progress)
    
    return {
        "position": (intercept_x, intercept_y),
        "altitude_km": altitude,
        "time": intercept_time,
        "phase": phase,
        "velocity_ms": velocity,
        "flight_progress": time_ratio
    }
```

---

##### **이론적 함의 및 시스템 설계 원칙**

**1. 단계별 차별화 원칙**:
```
각 비행 단계는 고유한 물리적 특성을 가지므로, 
요격 전략도 단계별로 차별화되어야 한다.

- 부스트: 탐지 및 추적에 집중
- 중간코스: 상층방어 최적 교전
- 종말: 하층방어 최종 방어선
```

**2. 다층 방어 시너지**:
```
상층방어와 하층방어는 서로 다른 비행 단계를 담당하여
시너지 효과를 창출한다.

P_total_defense = 1 - (1 - P_LSAM_midcourse) × (1 - P_MSAM_terminal)

예시:
  P_LSAM = 0.96 (중간코스)
  P_MSAM = 0.82 (종말)
  P_total = 1 - (1-0.96)×(1-0.82) = 0.9928 (99.28%)
```

**3. 시간 최적화 원칙**:
```
각 단계의 최적 교전 시점은 다음 요소의 균형점이다:

- 궤적 예측 정확도
- 교전 시간 창 길이
- 요격 확률
- 재교전 가능성

수학적 표현:
  t* = argmax [α·Accuracy(t) + β·Window(t) + γ·P_k(t) + δ·Reengagement(t)]
```

### ○ 위협도 산출 방법

#### ▪ 예상 탄착지점 기반 위험도 평가

위협도는 탄도미사일의 예상 탄착지점과 방어자산의 위치를 기반으로 산출된다.

**탄착지점 예측 모델**:
```
예상 탄착지점 = 발사 위치 + 비행 벡터 × 비행 시간
비행 벡터 = (목표 자산 위치 - 발사 위치) / |거리|
탄착 오차 = 원형공산오차(CEP)
```

**위험도 평가 기준**:
1. **직접 위협**: 탄착지점이 자산 위치로부터 CEP 이내
2. **간접 위협**: 탄착지점이 자산 위치로부터 CEP의 2배 이내
3. **잠재 위협**: 탄착지점이 자산 위치로부터 CEP의 3배 이내

**코드 구현** (`@config_mip.py:324-347`):
```python
@staticmethod
def create_priority_defense_assets() -> List[Dict]:
    """우선순위 기반 주요 방어 자산 10개 선택"""
    assets = [
        # 최고 우선순위 (Priority 1) - 국가 중추 기관
        {"id": "A01", "name": "Blue_House", "position": (0, 0), 
         "value": 1500, "priority": 1},
        {"id": "A02", "name": "Defense_Ministry", "position": (2, -1), 
         "value": 900, "priority": 1},
        {"id": "A03", "name": "Intelligence_Service", "position": (-3, 1), 
         "value": 850, "priority": 1},
        
        # 높은 우선순위 (Priority 2) - 전략적 가치
        {"id": "A04", "name": "Gyeryong_Command", "position": (15, -25), 
         "value": 950, "priority": 2},
        # ... (총 10개 자산)
    ]
    
    # 우선순위별 가중치 적용
    priority_weights = {1: 1.0, 2: 0.8, 3: 0.6, 4: 0.4}
    for asset in assets:
        asset["weighted_value"] = asset["value"] * priority_weights[asset["priority"]]
    
    return assets
```

#### ▪ 방어자산 범위 내 위협 식별

각 방어자산에 대해 위협하는 미사일을 식별하고 교전 가능한 요격체계를 매칭한다.

**교전 가능성 매트릭스 생성** (`@config_mip.py:733-796`):
```python
@staticmethod
def create_engagement_matrix() -> Dict:
    """교전 가능성 매트릭스 생성 (궤적 기반)"""
    batteries = BatteryDeploymentConfig.create_battery_deployment()
    threats = ThreatMissileConfig.create_ballistic_missile_threats()
    
    engagement_matrix = {}
    
    for battery in batteries:
        for threat in threats:
            key = (battery["id"], threat["id"])
            
            # 궤적 기반 상세 분석
            analysis = EngagementZoneConfig.can_engage_trajectory(
                battery, threat, None
            )
            engagement_matrix[key] = analysis["can_engage"]
    
    return engagement_matrix
```

**궤적 기반 교전 가능성 계산** (`@config_mip.py:671-722`):
```python
@staticmethod
def can_engage_trajectory(battery: Dict, threat: Dict) -> Dict:
    """궤적 기반 교전 가능성 및 교전 시간 윈도우 계산"""
    
    # 교전 가능 시간 윈도우 찾기
    flight_time = threat["flight_time"]
    engagement_windows = []
    
    # 3초 간격으로 궤적 상의 지점들 검사
    for t in range(0, int(flight_time), 3):
        intercept_point = calculate_trajectory_intercept_point(threat, t)
        
        # 거리 및 고도 계산
        distance = sqrt((battery_pos - intercept_point["position"])^2)
        altitude = intercept_point["altitude_km"]
        
        # 교전 가능 조건 확인
        if (min_range <= distance <= max_range and 
            min_altitude <= altitude <= max_altitude):
            engagement_windows.append({
                "time": t,
                "distance": distance,
                "altitude": altitude
            })
    
    return {
        "can_engage": len(engagement_windows) > 0,
        "engagement_windows": engagement_windows
    }
```

#### ▪ 자산가치(Bi) 기반 우선순위 결정

방어자산의 가치는 전략적 중요도와 우선순위를 반영하여 결정된다.

**자산 가치 체계**:
```
Priority 1 (국가 중추): 850~1,500
  - 청와대(1,500), 국방부(900), 국정원(850)

Priority 2 (전략 거점): 650~950
  - 계룡대(950), 평택미군기지(900), 오산공군기지(850)
  - 인천공항(800), 부산항(750), 인천항(700), 김포공항(650)
```

**가중치 적용**:
```python
priority_weights = {
    1: 1.0,  # 100% 가중치
    2: 0.8,  # 80% 가중치
    3: 0.6,  # 60% 가중치
    4: 0.4   # 40% 가중치
}

weighted_value = asset["value"] * priority_weights[asset["priority"]]
```

**목적함수에서의 활용** (`@문서/DWTA_문제정의_수학적표현.md:146-168`):
```
min Z = Σ B_i × D_i

where:
  B_i: 자산 i의 가치 (가중치 적용)
  D_i: 자산 i의 기댓값 손실 확률
  
D_i = Π_{t∈T_i} [Π_{u∈U} (1 - x_iu·k_iu·P_u) × Π_{l∈L} (1 - x_il·k_il·P_l)]
```

---

## 3.1.2. 교전 가능성 평가(Engagement Feasibility)

### ○ Time Window 산출 개념

교전 시간 창(Time Window)은 요격체계가 위협 미사일을 효과적으로 교전할 수 있는 시간 구간을 의미한다.

#### ▪ 탄도미사일 궤적(시간/위치)과 요격체계 교전영역의 교차점 계산

**교전 시간 창 계산 알고리즘** (`@config_mip.py:799-868`):
```python
@staticmethod
def calculate_engagement_time_window(battery: Dict, threat: Dict) -> Dict:
    """교전 타임 윈도우 계산 - 궤적 기반 실시간 분석"""
    
    engagement_opportunities = []
    
    # 미사일 궤적을 시간별로 샘플링 (0.5초 간격)
    time_step = 0.5  # 초
    for t in range(int(flight_time / time_step)):
        current_time = t * time_step
        
        # 현재 시점에서 미사일 위치 계산
        intercept_point = calculate_trajectory_intercept_point(threat, current_time)
        
        missile_pos = intercept_point["position"]
        missile_altitude = intercept_point["altitude_km"]
        
        # 포대에서 미사일까지의 거리
        distance_to_missile = sqrt(
            (missile_pos[0] - battery_pos[0])^2 + 
            (missile_pos[1] - battery_pos[1])^2
        )
        
        # 교전 가능 범위 내인지 확인
        if distance_to_missile <= battery_range:
            engagement_opportunities.append({
                "time": current_time,
                "distance": distance_to_missile,
                "altitude": missile_altitude
            })
    
    # 연속된 교전 기회 → 교전 윈도우
    engagement_window_sec = len(engagement_opportunities) * time_step
    
    return {
        "engagement_window_sec": engagement_window_sec,
        "can_engage": engagement_window_sec > 0
    }
```

**교전 시간 창의 물리적 의미**:
```
교전 시간 창 = 미사일이 요격체계의 교전 영역 내에 머무르는 시간

조건:
1. 거리 조건: min_range ≤ distance ≤ max_range
2. 고도 조건: min_altitude ≤ altitude ≤ max_altitude
3. 시간 조건: 발사 준비 시간 + 비행 시간 < 충돌 시간
```

#### ▪ 상층방어(L-SAM)와 하층방어(M-SAM)별 교전가능시간 도출

**L-SAM 교전 시간 창**:
```
최적 교전 윈도우: 35초
최소 교전 윈도우: 12초
교전 고도: 40~80km (중간코스 단계)
교전 거리: 150~300km
최적 교전 시점: 비행시간의 1/3 지점 (midcourse)
```

**M-SAM 교전 시간 창**:
```
최적 교전 윈도우: 20초
최소 교전 윈도우: 8초
교전 고도: 0.05~50km (종말 단계)
교전 거리: 5~50km
최적 교전 시점: 비행시간의 2/3 지점 (terminal)
```

**코드 구현** (`@config_mip.py:121-139`):
```python
engagement_window_config: Dict[str, Dict] = {
    "LSAM": {
        "optimal_window_sec": 35.0,      # 최적 교전 윈도우 (초)
        "minimum_window_sec": 12.0,      # 최소 교전 윈도우 (초)
        "maximum_range_km": 300,         # 최대 교전 거리 (km)
        "optimal_altitude_km": 55,       # 최적 교전 고도 (km)
        "window_efficiency_factor": 0.95  # 윈도우 효율성 계수
    },
    "MSAM": {
        "optimal_window_sec": 20.0,      # 최적 교전 윈도우 (초)
        "minimum_window_sec": 8.0,       # 최소 교전 윈도우 (초)
        "maximum_range_km": 50,          # 최대 교전 거리 (km)
        "optimal_altitude_km": 12.5,     # 최적 교전 고도 (km)
        "window_efficiency_factor": 0.92  # 윈도우 효율성 계수
    }
}
```

#### ▪ 시간-공간 제약조건 반영

**시간 제약**:
1. **발사 준비 시간**: 탐지 후 발사까지 최소 10초 소요
2. **요격체 비행 시간**: 거리에 따라 5~30초
3. **재교전 시간**: 요격 실패 시 재할당 및 재발사에 15초 이상 필요

**공간 제약**:
1. **교전 범위**: 요격체계별 최소/최대 교전 거리
2. **교전 고도**: 요격체계별 최소/최대 교전 고도
3. **기하학적 제약**: 요격 각도 및 상대 속도

**실시간 제약 검증** (`@문서/실시간_제약_및_SLS_상세분석.md:45-92`):
```python
# 교전 가능 시간 창 확인
W_ij(t) = {
    1  if t_min^ij ≤ t ≤ t_max^ij
    0  otherwise
}

where:
  t_min^ij: 위협 i에 대한 시스템 j의 최소 교전 시간
  t_max^ij: 위협 i에 대한 시스템 j의 최대 교전 시간
```

### ○ 명중률 계산 방법

#### ▪ Time Window 기반 동적 명중률 함수화

명중률은 교전 시간 창의 품질에 따라 동적으로 변화한다.

**K-factor (교전창 품질 변수) 도입**: `kij(t) = kmin + (kmax - kmin) × (1 - dij(t)/Rj)`

**수학적 정의** (`@문서/DWTA_문제정의_수학적표현.md:24-38`):
```
k_ij(t) = k_min + (k_max - k_min) × (1 - d_ij(t)/R_j)

where:
  k_ij(t) ∈ [0.6, 1.0]: 시간 t에서의 교전 창 품질
  d_ij(t): 시간 t에서 위협 i와 시스템 j 간 거리
  R_j: 시스템 j의 최대 교전 범위
  k_min = 0.6: 최악 조건 (교전 범위 경계)
  k_max = 1.0: 최적 조건 (최단 거리)
```

**물리적 의미**:
- 위협이 시스템에 가까울수록 k_ij(t) → 1.0 (최적 교전 조건)
- 위협이 교전 범위 경계에 있을 때 k_ij(t) → 0.6 (제한적 교전 조건)
- 교전 범위 밖: k_ij(t) = 0 (교전 불가)

**K-factor 이론적 근거** (`@k_factor_theory.py:45-72`):
```python
def calculate_theoretical_k_factor(self, engagement_window: EngagementWindow) -> KFactorComponents:
    """이론적 k-factor 계산"""
    
    # 1. 시간적 요소 (Temporal Factor)
    temporal_factor = self._calculate_temporal_factor(engagement_window)
    
    # 2. 기하학적 요소 (Geometric Factor)
    geometric_factor = self._calculate_geometric_factor(engagement_window)
    
    # 3. 운동학적 요소 (Kinematic Factor)
    kinematic_factor = self._calculate_kinematic_factor(engagement_window)
    
    # 4. 환경적 요소 (Environmental Factor)
    environmental_factor = self._calculate_environmental_factor(engagement_window)
    
    # 5. 종합 k-factor 계산 (가중 기하 평균)
    combined_factor = (
        (temporal ** 0.3) *
        (geometric ** 0.25) *
        (kinematic ** 0.25) *
        (environmental ** 0.2)
    ) ** (1.0 / 1.0)
    
    return combined_factor
```

**시간적 요소** (`@k_factor_theory.py:74-92`):
```python
def _calculate_temporal_factor(self, window: EngagementWindow) -> float:
    """교전 윈도우의 지속 시간이 길수록 요격 기회가 많아짐"""
    optimal_duration = 15.0  # 최적 교전 윈도우 (초)
    min_duration = 2.0       # 최소 교전 윈도우 (초)
    
    duration_ratio = min(window.duration / optimal_duration, 1.0)
    temporal_factor = k_min + (1.0 - k_min) * sqrt(duration_ratio)
    
    return min(temporal_factor, 1.0)
```

**기하학적 요소** (`@k_factor_theory.py:94-117`):
```python
def _calculate_geometric_factor(self, window: EngagementWindow) -> float:
    """요격 각도와 거리에 따른 기하학적 유리함"""
    
    # 요격 각도 효과 (정면 요격이 가장 유리)
    angle_factor = cos(radians(window.intercept_angle))
    
    # 거리 효과 (가우시안 분포)
    optimal_distance = 50.0  # km
    distance_deviation = abs(window.distance_to_battery - optimal_distance)
    distance_factor = exp(-(distance_deviation / optimal_distance)^2)
    
    geometric_factor = 0.7 + 0.3 * angle_factor * distance_factor
    return min(geometric_factor, 1.0)
```

#### ▪ 거리 종속형 격추확률 적용

**유효 요격 확률** (`@문서/DWTA_문제정의_수학적표현.md:67-85`):
```
P_ij^eff(t) = k_ij(t) × P_j^(m)

where:
  P_j^(m): 살보 요격 확률 (2발 발사)
  k_ij(t): 교전 창 품질 계수 (시간/거리 종속)
```

**예시 계산**:
```
LSAM_01 → T05 교전 (거리 50km, 최대 범위 150km)
├─ 단발 확률: p_u = 0.85
├─ 살보 확률: P_u^(2) = 1 - (1-0.85)² = 0.9775
├─ K-factor: k = 0.6 + 0.4 × (1 - 50/150) = 0.867
└─ 유효 확률: P_eff = 0.867 × 0.9775 = 0.847 (84.7%)
```

**코드 구현** (`@nonlinear_mip_optimizer.py:121-126`):
```python
# k 파라미터 범위 (교전 윈도우 기반 확률 보정 계수)
self.k_min = 0.6  # 하한: 최악 조건에서도 60% 효율 보장
self.k_max = 1.0  # 상한: 최적 조건에서 100% 효율

# 미사일 수 고정값
self.missiles_per_engagement = 2  # 교전 시 기본 2발 사용
```

#### ▪ 살보 확률 계산: `Pu(m) = 1 - (1 - pu)^m`

**살보 요격 확률 공식** (`@문서/DWTA_문제정의_수학적표현.md:43-63`):
```
단발 요격 확률:
  - L-SAM: p_u = 0.85 (85%)
  - M-SAM: p_l = 0.78 (78%)

살보 요격 확률 (m = 2발):
  P_j^(m) = 1 - (1 - p_j)^m

계산 결과:
  - L-SAM: P_u^(2) = 1 - (1 - 0.85)^2 = 1 - 0.0225 = 0.9775 (97.75%)
  - M-SAM: P_l^(2) = 1 - (1 - 0.78)^2 = 1 - 0.0484 = 0.9516 (95.16%)
```

**물리적 의미**:
```
살보 발사 = 2발의 미사일을 순차적으로 발사
생존 확률 = (1 - p)^m = 모든 미사일이 빗나갈 확률
격추 확률 = 1 - 생존 확률 = 최소 1발이 명중할 확률
```

**실제 운용 방식** (`@config_mip.py:24-28`):
```python
class SLSConfig(BaseModel):
    """Shoot-Look-Shoot 설정 (실제 살보 발사 방식)"""
    evaluation_delay_sec: float = 3.0  # 살보 발사 간격 (첫발 → 3초 → 둘째발)
    re_engagement_factor: float = 0.9  # 재교전 확률 보정 계수
    max_engagements_per_target: int = 2  # 표적당 최대 교전 횟수
```

### ○ 평가결과 데이터 구조

#### ▪ 탄도미사일별/요격체계별 Time Window 매트릭스 구성

**교전 시간 창 매트릭스 구조**:
```python
engagement_windows = {
    ("LSAM_01", "T01"): {
        "can_engage": True,
        "engagement_window_sec": 45.2,
        "window_quality": 0.95,
        "optimal_time": 160.0,
        "engagement_opportunities": [
            {"time": 155.0, "distance": 120.5, "altitude": 55.2},
            {"time": 155.5, "distance": 118.3, "altitude": 56.1},
            # ... (0.5초 간격 샘플)
        ]
    },
    ("LSAM_01", "T02"): {
        "can_engage": False,
        "engagement_window_sec": 0.0,
        "window_quality": 0.0
    },
    # ... (10개 배터리 × 15개 위협 = 150개 조합)
}
```

**윈도우 품질 평가** (`@config_mip.py:872-888`):
```python
# 윈도우 품질 계산
if engagement_window_sec >= optimal_window:
    window_quality = 1.0  # 최적 윈도우
elif engagement_window_sec >= minimum_window:
    # 선형 보간
    window_quality = (engagement_window_sec - minimum_window) / \
                     (optimal_window - minimum_window)
else:
    # 최소 윈도우 미달
    window_quality = 0.2 * (engagement_window_sec / minimum_window)
```

#### ▪ 명중률 매트릭스 저장 구조

**K-factor 캐시 구조** (`@config_mip.py`):
```python
class KFactorCache:
    """K-factor 사전 계산 캐시"""
    
    def __init__(self, k_min=0.6, k_max=1.0):
        self.k_min = k_min
        self.k_max = k_max
        self.cache = {}  # {(threat_id, system_id): k_value}
    
    def precompute(self, threats, interceptor_systems, engagement_matrix):
        """모든 교전 가능 조합에 대해 k-factor 사전 계산"""
        for threat in threats:
            for system in interceptor_systems:
                key = (threat.id, system.id)
                
                # 교전 가능성 확인
                if not engagement_matrix.can_engage(system.id, threat.id):
                    self.cache[key] = self.k_min
                    continue
                
                # K-factor 계산
                k_value = self._calculate_k_value(threat, system)
                self.cache[key] = k_value
    
    def get(self, threat_id, system_id):
        """캐시된 k-factor 조회"""
        return self.cache.get((threat_id, system_id), self.k_min)
```

**명중률 매트릭스**:
```python
intercept_probability_matrix = {
    ("LSAM_01", "T01"): {
        "base_pk_single": 0.85,
        "salvo_pk": 0.9775,
        "k_factor": 0.867,
        "effective_pk": 0.847,
        "distance": 50.0,
        "window_quality": 0.95
    },
    ("MSAM_01", "T01"): {
        "base_pk_single": 0.78,
        "salvo_pk": 0.9516,
        "k_factor": 0.723,
        "effective_pk": 0.688,
        "distance": 35.0,
        "window_quality": 0.82
    },
    # ... (모든 교전 가능 조합)
}
```

#### ▪ 3차원 시각화 개념도

**시각화 차원**:
1. **X축**: 시간 (초) - 시뮬레이션 진행 시간
2. **Y축**: 위협 미사일 ID - 15개 위협
3. **Z축**: 명중률 (%) - 0~100%

**시각화 요소**:
```
1. 교전 시간 창 (Time Window)
   - 색상: 녹색 (교전 가능), 빨강 (교전 불가)
   - 두께: 윈도우 품질 (0.6~1.0)

2. 명중률 곡선
   - 높이: 유효 명중률 (k_factor × salvo_pk)
   - 색상 그라데이션: 낮음(빨강) → 높음(녹색)

3. 할당 상태
   - 마커: 할당된 배터리 표시
   - 선: 배터리-위협 연결선

4. 교전 이벤트
   - 점: 요격 시도 시점
   - 별: 요격 성공
   - X: 요격 실패
```

**데이터 구조**:
```python
visualization_data = {
    "time_series": np.arange(0, 1600, 1),  # 1초 간격
    "threats": ["T01", "T02", ..., "T15"],
    "intercept_probability": np.array([
        [0.0, 0.0, ..., 0.847, ..., 0.0],  # T01의 시간별 명중률
        [0.0, 0.0, ..., 0.723, ..., 0.0],  # T02의 시간별 명중률
        # ... (15개 위협)
    ]),
    "engagement_windows": [
        {"threat": "T01", "battery": "LSAM_01", 
         "start": 155.0, "end": 200.2, "quality": 0.95},
        # ... (모든 교전 창)
    ],
    "events": [
        {"time": 180.5, "threat": "T01", "type": "INTERCEPT_SUCCESS"},
        {"time": 195.2, "threat": "T03", "type": "INTERCEPT_MISS"},
        # ... (모든 이벤트)
    ]
}
```

**3D 플롯 생성 코드 (개념)**:
```python
import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D

fig = plt.figure(figsize=(16, 10))
ax = fig.add_subplot(111, projection='3d')

# 시간-위협-명중률 3D 표면
X, Y = np.meshgrid(time_series, range(len(threats)))
Z = intercept_probability

surf = ax.plot_surface(X, Y, Z, cmap='RdYlGn', alpha=0.7)

# 교전 창 표시 (반투명 박스)
for window in engagement_windows:
    # ... 박스 그리기

# 이벤트 마커
for event in events:
    # ... 마커 표시

ax.set_xlabel('Time (sec)')
ax.set_ylabel('Threat ID')
ax.set_zlabel('Intercept Probability (%)')
ax.set_title('Time-Based Weapon Recommendation Process - 3D Visualization')

plt.colorbar(surf, label='Effective Pk')
plt.show()
```

---

## 요약

본 절에서는 시간 개념을 적용한 무기추천 프로세스 모형을 정립하였다.

**핵심 성과**:

1. **위협 평가**: 탄도미사일 종류별 특성 분석, 3단계 비행 모델 정립, 자산가치 기반 우선순위 결정
2. **교전 가능성 평가**: 궤적 기반 Time Window 산출, K-factor 도입을 통한 동적 명중률 함수화, 살보 확률 계산
3. **데이터 구조**: 교전 시간 창 매트릭스, 명중률 매트릭스, 3차원 시각화 체계 구축

**학술적 기여**:
- 시간 종속 교전 창 모델링의 수학적 정식화
- K-factor 이론의 물리적 근거 제시
- 실시간 DWTA 최적화를 위한 데이터 구조 설계

**실무적 적용성**:
- 실제 무기체계 스펙 반영 (L-SAM, M-SAM)
- 북한 탄도미사일 위협 시나리오 기반 검증
- 실시간 시뮬레이션 환경에서의 구현 및 검증 완료
