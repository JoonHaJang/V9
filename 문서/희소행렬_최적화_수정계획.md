# 희소 행렬 + 비트 연산 최적화 수정 계획

## 수정 개요
- **목표**: `primary_assignments` 딕셔너리를 희소 행렬 + 비트마스크 기반으로 교체
- **예상 작업 시간**: 8-10시간
- **수정 파일**: 2개 (신규 1개 + 기존 1개)

---

## Phase 1: 새 클래스 구현

### 파일: `sparse_assignment_manager.py` (신규)
- **위치**: `c:\Users\Joon\Downloads\v6\sparse_assignment_manager.py`
- **라인 수**: ~200 라인
- **작업 시간**: 2-3시간

**구현 메서드**:
1. `__init__(n_threats, n_batteries)` - 초기화
2. `assign(threat_id, battery_id)` - 할당
3. `unassign(threat_id, battery_id)` - 할당 해제
4. `is_assigned(threat_id, battery_id)` - 할당 확인
5. `get_battery_load(battery_id)` - 배터리 부하
6. `get_assigned_threats(battery_id)` - 배터리별 할당 위협 목록
7. `get_batteries_for_threat(threat_id)` - 위협별 할당 배터리 목록
8. `get_total_assignments()` - 전체 할당 개수
9. `clear()` - 초기화
10. `merge_assignments(new_assignments)` - 할당 병합

---

## Phase 2: multi_missile_tracker_gui.py 수정

### 2.1 초기화 부분 (3곳)

**위치 1**: Line 721
```python
# 🔧 BEFORE
self.primary_assignments = {}

# 🔧 AFTER
self.assignment_manager = SparseAssignmentManager(
    n_threats=100,  # 최대 위협 수
    n_batteries=20   # 최대 배터리 수
)
```

**위치 2**: Line 923
```python
# 🔧 BEFORE
self.primary_assignments = {}

# 🔧 AFTER
self.assignment_manager.clear()
```

**위치 3**: Line 551, 638
```python
# 🔧 BEFORE
self.tracker.primary_assignments.clear()

# 🔧 AFTER
self.tracker.assignment_manager.clear()
```

---

### 2.2 할당 조회 패턴 (15곳)

**패턴**: `for battery_id, threat_list in self.primary_assignments.items():`

**위치 1**: Line 1038-1048 (궤적 이탈 시 할당 제거)
```python
# 🔧 BEFORE
for battery_id, threat_list in list(self.primary_assignments.items()):
    if missile_id in threat_list:
        self.primary_assignments[battery_id].remove(missile_id)
        if not self.primary_assignments[battery_id]:
            del self.primary_assignments[battery_id]
        break

# 🔧 AFTER
assigned_batteries = self.assignment_manager.get_batteries_for_threat(missile_id)
for battery_id in assigned_batteries:
    self.assignment_manager.unassign(missile_id, battery_id)
```

**위치 2**: Line 1079-1081 (할당된 배터리 찾기)
```python
# 🔧 BEFORE
assigned_batteries = []
for battery_id, threat_list in self.primary_assignments.items():
    if missile_id in threat_list:
        assigned_batteries.append(battery_id)

# 🔧 AFTER
assigned_batteries = self.assignment_manager.get_batteries_for_threat(missile_id)
```

**위치 3**: Line 1203 (담당 배터리 찾기)
```python
# 🔧 BEFORE
battery_list = [bid for bid, threat_list in self.primary_assignments.items() if missile_id in threat_list]

# 🔧 AFTER
battery_list = self.assignment_manager.get_batteries_for_threat(missile_id)
```

**위치 4**: Line 1212-1217 (요격 성공 시 할당 제거)
```python
# 🔧 BEFORE
for battery_id, threat_list in list(self.primary_assignments.items()):
    if missile_id in threat_list:
        self.primary_assignments[battery_id].remove(missile_id)
        if not self.primary_assignments[battery_id]:
            del self.primary_assignments[battery_id]
        released_batteries += 1

# 🔧 AFTER
assigned_batteries = self.assignment_manager.get_batteries_for_threat(missile_id)
for battery_id in assigned_batteries:
    self.assignment_manager.unassign(missile_id, battery_id)
    released_batteries += 1
```

**위치 5**: Line 1233 (할당된 위협 ID 수집)
```python
# 🔧 BEFORE
assigned_threat_ids = set()
for threat_list in self.primary_assignments.values():
    assigned_threat_ids.update(threat_list)

# 🔧 AFTER
assigned_threat_ids = self.assignment_manager.get_all_assigned_threats()
```

**위치 6**: Line 1261 (담당 배터리 찾기)
```python
# 🔧 BEFORE
battery_list = [bid for bid, threat_list in self.primary_assignments.items() if missile_id in threat_list]

# 🔧 AFTER
battery_list = self.assignment_manager.get_batteries_for_threat(missile_id)
```

**위치 7**: Line 1287 (담당 배터리 찾기)
```python
# 🔧 BEFORE
battery_list = [bid for bid, threat_list in self.primary_assignments.items() if missile_id in threat_list]

# 🔧 AFTER
battery_list = self.assignment_manager.get_batteries_for_threat(missile_id)
```

**나머지 8곳**: 동일한 패턴으로 수정

---

### 2.3 할당 개수 세기 (8곳)

**패턴**: `sum(len(threats) for threats in self.primary_assignments.values())`

**위치 1**: Line 1223
```python
# 🔧 BEFORE
current_assignments = sum(len(threats) for threats in self.primary_assignments.values())

# 🔧 AFTER
current_assignments = self.assignment_manager.get_total_assignments()
```

**나머지 7곳**: 동일한 패턴으로 수정

---

### 2.4 배터리별 할당 조회 (4곳)

**패턴**: `self.primary_assignments.get(battery_id, [])`

**위치 1**: Line 1519
```python
# 🔧 BEFORE
assigned_threats = self.primary_assignments.get(battery_id, [])

# 🔧 AFTER
assigned_threats = self.assignment_manager.get_assigned_threats(battery_id)
```

**나머지 3곳**: 동일한 패턴으로 수정

---

### 2.5 할당 병합 로직 (1곳)

**위치**: Line 1878-1917 (약 40라인)

```python
# 🔧 BEFORE (40 lines)
active_threat_ids = {tid for tid, m in self.missiles.items() if m.get('active', False)}

if self.primary_assignments and isinstance(list(self.primary_assignments.values())[0], str):
    old_structure = self.primary_assignments.copy()
    self.primary_assignments = {}
    for bid, tid in old_structure.items():
        if tid in active_threat_ids:
            self.primary_assignments[bid] = [tid]
else:
    for bid in list(self.primary_assignments.keys()):
        self.primary_assignments[bid] = [tid for tid in self.primary_assignments[bid] if tid in active_threat_ids]
        if not self.primary_assignments[bid]:
            del self.primary_assignments[bid]

old_assignment_count = sum(len(threats) for threats in self.primary_assignments.values())

for battery_id, threat_list in new_assignments.items():
    if battery_id not in self.primary_assignments:
        self.primary_assignments[battery_id] = []
    self.primary_assignments[battery_id] = threat_list

total_assignment_count = sum(len(threats) for threats in self.primary_assignments.values())

# 🔧 AFTER (5 lines)
active_threat_ids = {tid for tid, m in self.missiles.items() if m.get('active', False)}

old_assignment_count = self.assignment_manager.get_total_assignments()
self.assignment_manager.merge_assignments(new_assignments, active_threat_ids)
total_assignment_count = self.assignment_manager.get_total_assignments()
```

---

## Phase 3: 통합 테스트

### 테스트 시나리오
1. **LIGHT_5**: 5개 위협 (기본 기능 검증)
2. **BASELINE_15**: 15개 위협 (성능 벤치마크)
3. **LARGE_30**: 30개 위협 (확장성 검증)

### 검증 항목
- [ ] 할당/해제 정상 동작
- [ ] 요격 성공/실패 처리
- [ ] 재할당 로직
- [ ] 성능 개선 확인 (벤치마크)

---

## 수정 추적 태그

모든 수정 사항에 다음 태그 추가:
- `# 🔧 SPARSE_OPT: [설명]` - 희소 행렬 최적화 관련 수정
- `# 🔧 BEFORE` - 기존 코드
- `# 🔧 AFTER` - 수정된 코드

---

## 체크리스트

### Phase 1
- [ ] `sparse_assignment_manager.py` 생성
- [ ] 10개 메서드 구현
- [ ] 단위 테스트 작성

### Phase 2
- [ ] 초기화 부분 수정 (3곳)
- [ ] 할당 조회 패턴 수정 (15곳)
- [ ] 할당 개수 세기 수정 (8곳)
- [ ] 배터리별 조회 수정 (4곳)
- [ ] 할당 병합 로직 수정 (1곳)

### Phase 3
- [ ] LIGHT_5 테스트
- [ ] BASELINE_15 테스트
- [ ] LARGE_30 테스트
- [ ] 성능 벤치마크

---

**작성일**: 2026-01-16
**작성자**: AI Assistant
